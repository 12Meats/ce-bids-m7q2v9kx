'use strict';

// screens/bids.js — Task 6: the bid list, search, and the New bid button.
// Until then this renderer is deliberately empty: the screen's placeholder
// markup lives in index.html and simply stays on screen.

function renderBids() {
  // Task 6 fills #bidsContent.
}

registerScreen('bids', { id: 'screen-bids', title: 'Bids', back: null, tab: 'bids', render: renderBids });
