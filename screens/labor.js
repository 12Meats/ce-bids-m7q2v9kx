'use strict';

// screens/labor.js — Task 9: crew, days, rentals, and equipment.
// Until then this renderer is deliberately empty: the screen's placeholder
// markup lives in index.html and simply stays on screen.

function renderLabor() {
  // Task 9 fills #laborContent.
}

registerScreen('labor', { id: 'screen-labor', title: 'Labor', back: 'bid', tab: 'bids', render: renderLabor });
