'use strict';

// screens/settings.js — Task 13: company details, crew, rates, clauses, and backups.
// Until then this renderer is deliberately empty: the screen's placeholder
// markup lives in index.html and simply stays on screen.

function renderSettings() {
  // Task 13 fills #settingsContent.
}

registerScreen('settings', { id: 'screen-settings', title: 'Settings', back: null, tab: 'settings', render: renderSettings });
