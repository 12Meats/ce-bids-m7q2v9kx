'use strict';

// screens/walk.js — Task 8: areas, materials off the catalog, and photos.
// Until then this renderer is deliberately empty: the screen's placeholder
// markup lives in index.html and simply stays on screen.

function renderWalk() {
  // Task 8 fills #walkContent.
}

registerScreen('walk', { id: 'screen-walk', title: 'Walkthrough', back: 'bid', tab: 'bids', render: renderWalk });
