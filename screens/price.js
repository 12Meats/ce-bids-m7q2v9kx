'use strict';

// screens/price.js — Task 10: the cost stack and the three price handles.
// Until then this renderer is deliberately empty: the screen's placeholder
// markup lives in index.html and simply stays on screen.

function renderPrice() {
  // Task 10 fills #priceContent.
}

registerScreen('price', { id: 'screen-price', title: 'Price', back: 'bid', tab: 'bids', render: renderPrice });
